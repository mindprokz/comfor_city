|<?php 
|  $part = array(
|    'post_type' => 'blog', //Тип поста
|    'posts_per_page' => -1,//Постов на одной странице
|    'category_name' => 'blog' //Категория постов
|  );
|?>

a(name="blog")
section.main
  .container
    img(src!="<?php echo get_template_directory_uri();?>/images/logo.png")
    h3!="Полезная информация о недвижимости"
    ul.half
      |<?php
      |  $lastBlog = new WP_Query($part);
      |  if( $lastBlog->have_posts() ):
      |    while($lastBlog->have_posts() ): $lastBlog->the_post();
      |?>
      |     <?php $file = types_render_field( "file_blog", array('raw' => true) ); ?>
      a(href!="<?php echo $file; ?>" target="_blank").blog
        li!="<?php the_title(); ?>"
      |<?php
      |    endwhile;
      |  endif;
      |?>
  footer.blue
